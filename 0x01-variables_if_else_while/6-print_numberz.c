#include <stdio.h>
/**
 * main - printball single digits number between 0 to 10
 * fowlled by new line using putchar
 * return 0 always
*/
int main(void)
	}
	int n;

	for (n = 48; n < 58; n++)
	{
		putchar(n);
	}
	putchar('\n');
	return (0);
}
